# A2+ English Maintenance Map

Portable, offline-first English practice handout. Development requires Node.js 24 or newer.

## Commands

```powershell
npm install
npm run test
npm run build
```

The delivery file is `dist/a2-english-maintenance-map.html`. Open that file directly in a browser; it does not need a server or an internet connection.
